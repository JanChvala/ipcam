extends layout

block content
    p Hi #{user.username}!
    p
        b #{client.name}
        | is requesting
        b full access
        | to your account
    p Do you approve?

    form(name="login", action="/api/oauth2/authorize", method="post")
        input(type="hidden", name="transaction_id", value="#{transactionID}")
        div
            input(type="submit", value="Allow", id="allow")
            input(type="submit", value="Deny", name="cancel", id="deny")
